version: 2
jobs:
 build:
  environment:
   cumulusci.core.keychain.EnvironmentProjectKeychain
  machine: 
   python:
    version: 3.7.0
  steps:
  - run :  'pip install --upgrade pip'
  - run :   'pip install --upgrade cumulusci'
  - run :   'if [[ $CIRCLE_BRANCH == "master" ]]; then cci flow run ci_master --org avalara; fi'
workflows:
  version: 2
  build:
    jobs:
      - build